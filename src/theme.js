import { createTheme } from "@mui/material";

export const theme = createTheme({
    pallete:{
        primary:{
            main: "#222831"
            // light: "#00ADB5"
        },
        secondary:{
            main: "#EEEEEE"
        },
        themeGray:{
            main: "#999"
        }
    }
})